indices:
  mysite:
    include:
      - /pod-report
    target: /query-index.xlsx
    properties:
      author:
        select: head > meta[property="og:title"]
        value: |
          attribute(el,'content')
      url:
        select: main a[href="/pod-members.json"]
        value: |
          innerHTML(el)
